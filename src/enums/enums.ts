export enum RepeatedTaskType {
  EveryDay = "Every day",
  Weekends = "Weekends",
  WeekDays = "Weekdays",
  Custom = "Custom"
}

export enum WeekDays {
  Monday = 1,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
  Sunday
}